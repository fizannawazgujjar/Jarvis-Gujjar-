from jinja2 import Template

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "jarvis.fullname" . }}
  labels:
    app: {{ include "jarvis.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "jarvis.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "jarvis.name" . }}
    spec:
      containers:
        - name: backend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
